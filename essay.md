SOLUTION TO ESSAY ON PROTEIN WEIGHT CALCULATOR:

The import function was used to import the table of the aminoacids with the columns (aminoacid, 1-Letter Code, 3-Letter Code, and weight_Da. I called the table ''aminoacids''

The function was named ''calculate_weight'', which will take the parameter called "my_protein"

The weight_Da column was assigned to the variable weight and 1-Letter Code to the variable aminoacid_name

The function strsplit() was used to split the string ''my_protein"

The counter protein_weight <- 0 was initialised thereafter.

To iterate on each item in the list, for loop was used. . The if else statement was used to set the criteria that the item should be in the list in order to calculate the weight and the sum of the weights.

Finally, the function returns the total weight divided by 1000 to convert the weight from dalton to kDa
